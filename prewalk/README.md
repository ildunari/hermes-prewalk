# Prewalk plugin files

This directory is the installable Hermes plugin. For documentation, installation, presets, and verification details, see the [repository README](../README.md).

Direct installer command:

```bash
hermes plugins install ildunari/hermes-prewalk/prewalk --enable
```
